import { extractArgTypes } from '@pxtrn/storybook-addon-docs-stencil'
import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'
import { html, render } from 'lit-html'

<Meta
    title="MDX/Button"
    component="rux-button"
    argTypes={extractArgTypes('rux-button')}
/>

# Button

<div>
<rux-tabs small id="tab-set-id-1">
<rux-tab id="tab-id-1">Vue</rux-tab>
<rux-tab id="tab-id-2">React</rux-tab>
<rux-tab id="tab-id-3">Angular</rux-tab>
</rux-tabs>

<rux-tab-panels aria-labelledby="tab-set-id-1">
  <rux-tab-panel aria-labelledby="tab-id-1">
  </rux-tab-panel>
  <rux-tab-panel aria-labelledby="tab-id-2">Tab 2 HTML content</rux-tab-panel>
  <rux-tab-panel aria-labelledby="tab-id-3">Tab 3 HTML content</rux-tab-panel>
</rux-tab-panels>
</div>

export const StandardButton = (args) => {
    return html`
        <div style="padding: 10%; display: flex; justify-content: center;">
            <rux-button
                ?disabled="${args.disabled}"
                ?icon-only="${args.iconOnly}"
                ?outline="${args.outline}"
                .size="${args.size}"
                .icon="${args.withIcon ? 'settings' : null}"
                >Button</rux-button
            >
        </div>
    `
}
;<Story name="positive">{StandardButton.bind({})}</Story>

<Canvas>
    <Story name="positive">{StandardButton.bind()}</Story>
</Canvas>

## Tree Shaking

```html
import { RuxButtonGroup } from 'astro-web-components/dist/custom-elements'
customElements.define('rux-button-group', RuxButtonGroup)
```
